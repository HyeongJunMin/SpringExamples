package com.hjmin.jpaboard.repository;

import com.hjmin.jpaboard.domain.MarketEmployee;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MarketEmployeeRepository extends JpaRepository<MarketEmployee, Long> {
}
